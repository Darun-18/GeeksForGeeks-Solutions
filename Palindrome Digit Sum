class Solution {
    boolean isDigitSumPalindrome(int n) {
        // code here
        int sum = 0;
        int temp =n;
        while(n>0){
            sum = sum+n%10;
            n=n/10;
        }
        
            int rev = 0;
            int rem = 0;
            int temp1=sum;
            while(sum>0){
                rem = sum%10;
                rev = rev*10 +rem;
                sum=sum/10;
            }
            if(temp1 == rev){
            
             return true;
            }
            
             return false;
            
                
            }
    }
